<view class="container">
  <% components.forEach(function(component, index) { %>
  <<%= component.name %> class="component" id="<%= component.dataConfigKey%>" bind:inited="onInited" config-data="{{<%= component.dataConfigKey %>}}" />
  <% }); %>
</view>
